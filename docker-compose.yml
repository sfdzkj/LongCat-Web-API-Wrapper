services:
  longcat-api:
    build: .
    container_name: longcat-api
    restart: unless-stopped
    ports:
      - "8082:8082"
    volumes:
      - ./data:/app/data
    environment:
      - VERBOSE=true